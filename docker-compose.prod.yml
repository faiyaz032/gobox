services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: gobox-backend-prod
    restart: always
    ports:
      - "${SERVER_PORT:-8010}:8010"
    env_file:
      - .env.prod
    environment:
      POSTGRES_HOST: postgres
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - gobox-network

  postgres:
    image: postgres
    container_name: gobox-postgres-prod
    restart: always
    env_file:
      - .env.prod
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - gobox-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

networks:
  gobox-network:
    driver: bridge
